# image used to build the base container
BASE_OS_IMAGE="paas/minimal:latest"
# image with all dependencies of all gc crawlers
BASE_CRAWLER_IMAGE="advana/gc-crawler:latest"
# image for running covid gc crawler
COVID_DOWNLOADER_IMAGE="advana/gc-covid-downloader:latest"
# image for running core gc crawler
CORE_DOWNLOADER_IMAGE="advana/gc-downloader:latest"
# image for the scanner uploader
SCANNER_UPLOADER_CONTAINER_IMAGE="paas/dlp:latest"